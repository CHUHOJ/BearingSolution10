<metro:MetroWindow x:Class="PrismMahappTest.Views.MainWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:infConstants="clr-namespace:PrismMahappTest.Infrastructure.Constants;assembly=PrismMahappTest.Infrastructure"
        xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="350" Width="525">

    <!-- Left-Windows Commands -->
    <metro:MetroWindow.LeftWindowCommands>
        <metro:WindowCommands x:Name="leftWindowsCommandRegion" prism:RegionManager.RegionName="LeftWindowCommandsRegions" />
    </metro:MetroWindow.LeftWindowCommands>

    <!-- Right-Windows Commands -->
    <metro:MetroWindow.RightWindowCommands>
        <metro:WindowCommands x:Name="rightWindowsCommandRegion" prism:RegionManager.RegionName="RightWindowCommandsRegions" />
    </metro:MetroWindow.RightWindowCommands>

    <!-- Flyouts -->
    <metro:MetroWindow.Flyouts>
        <metro:FlyoutsControl x:Name="flyoutsControlRegion" />
    </metro:MetroWindow.Flyouts>
    


        
        <Grid>
        <ItemsControl prism:RegionManager.RegionName="{x:Static infConstants:RegionNames.MainRegion}" />
    </Grid>
</metro:MetroWindow>
